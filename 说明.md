2026-04 会话分析体系（analytics_*）：
说明：
1) 新的阅读统计统一走 `POST /api/analytics.php`。
2) 旧的 `tracking.php / behavior_events.php` 保留兼容，不再作为新的主统计链路。
3) Batch 1 报表、章节完成率、词位去重、回访判断都以 `analytics_*` 表为准。
4) `target_key / target_label / completed_target / completed_target_at` 已停用，当前代码不再写入这组字段。

一、建表 SQL

1) 会话主表（Batch 1 的主表，一行一个 session）：
```sql
CREATE TABLE IF NOT EXISTS analytics_sessions (
  session_id VARCHAR(80) NOT NULL,
  anonymous_user_id VARCHAR(80) NOT NULL,
  session_started_at DATETIME NOT NULL,
  first_word_tap_at DATETIME DEFAULT NULL,
  first_word_tap_sec INT UNSIGNED DEFAULT NULL,
  active_duration_ms BIGINT UNSIGNED NOT NULL DEFAULT 0,
  is_return_visit TINYINT(1) NOT NULL DEFAULT 0,
  previous_session_at DATETIME DEFAULT NULL,
  return_gap_hours DECIMAL(10,2) DEFAULT NULL,
  return_gap_days DECIMAL(10,2) DEFAULT NULL,
  completed_overall_rate DECIMAL(7,4) NOT NULL DEFAULT 0,
  last_content_event_at DATETIME DEFAULT NULL,
  dropoff_event_type ENUM('page_view','word_tap') DEFAULT NULL,
  dropoff_book_id VARCHAR(80) DEFAULT NULL,
  dropoff_chapter_no INT UNSIGNED DEFAULT NULL,
  dropoff_content_version VARCHAR(120) DEFAULT NULL,
  dropoff_page_no INT UNSIGNED DEFAULT NULL,
  dropoff_line_index INT UNSIGNED DEFAULT NULL,
  dropoff_position_key VARCHAR(120) DEFAULT NULL,
  dropoff_word VARCHAR(255) DEFAULT NULL,
  session_ended_at DATETIME DEFAULT NULL,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL,
  PRIMARY KEY (session_id),
  KEY idx_analytics_sessions_anonymous (anonymous_user_id),
  KEY idx_analytics_sessions_started (session_started_at),
  KEY idx_analytics_sessions_return (is_return_visit)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

如果你已经创建过 `analytics_sessions` 旧版表结构，请补充：
```sql
ALTER TABLE analytics_sessions
  ADD COLUMN last_content_event_at DATETIME DEFAULT NULL AFTER completed_overall_rate,
  ADD COLUMN dropoff_event_type ENUM('page_view','word_tap') DEFAULT NULL AFTER last_content_event_at,
  ADD COLUMN dropoff_book_id VARCHAR(80) DEFAULT NULL AFTER dropoff_event_type,
  ADD COLUMN dropoff_chapter_no INT UNSIGNED DEFAULT NULL AFTER dropoff_book_id,
  ADD COLUMN dropoff_content_version VARCHAR(120) DEFAULT NULL AFTER dropoff_chapter_no,
  ADD COLUMN dropoff_page_no INT UNSIGNED DEFAULT NULL AFTER dropoff_content_version,
  ADD COLUMN dropoff_line_index INT UNSIGNED DEFAULT NULL AFTER dropoff_page_no,
  ADD COLUMN dropoff_position_key VARCHAR(120) DEFAULT NULL AFTER dropoff_line_index,
  ADD COLUMN dropoff_word VARCHAR(255) DEFAULT NULL AFTER dropoff_position_key;
```

2) 内容元数据表（章节总可点击词位分母）：
```sql
CREATE TABLE IF NOT EXISTS analytics_content_chapters (
  book_id VARCHAR(80) NOT NULL,
  chapter_no INT UNSIGNED NOT NULL,
  content_version VARCHAR(120) NOT NULL,
  total_interactive_positions INT UNSIGNED NOT NULL DEFAULT 0,
  content_url VARCHAR(255) DEFAULT NULL,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL,
  PRIMARY KEY (book_id, chapter_no, content_version)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

3) 会话-章节表（每章完成率）：
```sql
CREATE TABLE IF NOT EXISTS analytics_session_chapters (
  session_id VARCHAR(80) NOT NULL,
  anonymous_user_id VARCHAR(80) NOT NULL,
  book_id VARCHAR(80) NOT NULL,
  chapter_no INT UNSIGNED NOT NULL,
  content_version VARCHAR(120) NOT NULL,
  total_interactive_positions INT UNSIGNED NOT NULL DEFAULT 0,
  unique_tapped_positions INT UNSIGNED NOT NULL DEFAULT 0,
  completion_rate DECIMAL(7,4) NOT NULL DEFAULT 0,
  is_completed TINYINT(1) NOT NULL DEFAULT 0,
  first_tap_at DATETIME DEFAULT NULL,
  last_tap_at DATETIME DEFAULT NULL,
  completed_at DATETIME DEFAULT NULL,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL,
  PRIMARY KEY (session_id, book_id, chapter_no, content_version),
  KEY idx_analytics_session_chapters_anonymous (anonymous_user_id),
  KEY idx_analytics_session_chapters_book (book_id, chapter_no)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

4) 会话-章节-词位表（同一词位只算一次）：
```sql
CREATE TABLE IF NOT EXISTS analytics_session_chapter_positions (
  session_id VARCHAR(80) NOT NULL,
  anonymous_user_id VARCHAR(80) NOT NULL,
  book_id VARCHAR(80) NOT NULL,
  chapter_no INT UNSIGNED NOT NULL,
  content_version VARCHAR(120) NOT NULL,
  position_key VARCHAR(120) NOT NULL,
  page_no INT UNSIGNED NOT NULL,
  line_index INT UNSIGNED DEFAULT NULL,
  interactive_index_in_line INT UNSIGNED DEFAULT NULL,
  word VARCHAR(255) DEFAULT NULL,
  first_tapped_at DATETIME NOT NULL,
  created_at DATETIME NOT NULL,
  PRIMARY KEY (session_id, book_id, chapter_no, content_version, position_key),
  KEY idx_analytics_positions_anonymous (anonymous_user_id),
  KEY idx_analytics_positions_book (book_id, chapter_no)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

5) 事件明细表（Phase 2 的原始事实表）：
```sql
CREATE TABLE IF NOT EXISTS analytics_events (
  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  event_type ENUM(
    'session_start',
    'return_visit',
    'page_view',
    'word_tap',
    'page_dwell',
    'audio_play_started',
    'audio_play_completed',
    'start_reading_click',
    'onboarding_to_story_click',
    'next_chapter_click',
    'content_load_failed',
    'session_interrupted',
    'chapter_complete',
    'parent_feedback',
    'more_chapters_response',
    'session_end'
  ) NOT NULL,
  dedupe_key VARCHAR(191) DEFAULT NULL,
  session_id VARCHAR(80) NOT NULL,
  anonymous_user_id VARCHAR(80) NOT NULL,
  book_id VARCHAR(80) DEFAULT NULL,
  chapter_no INT UNSIGNED DEFAULT NULL,
  content_version VARCHAR(120) DEFAULT NULL,
  page_no INT UNSIGNED DEFAULT NULL,
  position_key VARCHAR(120) DEFAULT NULL,
  word VARCHAR(255) DEFAULT NULL,
  liked ENUM('yes','no') DEFAULT NULL,
  comment VARCHAR(500) DEFAULT NULL,
  more_chapters_response ENUM('yes','no') DEFAULT NULL,
  more_chapters_note VARCHAR(500) DEFAULT NULL,
  payload_json LONGTEXT DEFAULT NULL,
  event_at DATETIME NOT NULL,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL,
  PRIMARY KEY (id),
  UNIQUE KEY uniq_analytics_events_dedupe (dedupe_key),
  KEY idx_analytics_events_session (session_id, event_at),
  KEY idx_analytics_events_type (event_type, event_at),
  KEY idx_analytics_events_book (book_id, chapter_no, event_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

二、核心定义

1) `session_started_at`
   - 页面进入应用时记录
2) `first_word_tap_sec`
   - 第一个可点读单词点击时间 - session_started_at
   - 直接由第一条 `word_tap` 事件回填，不依赖前端并行传字段
3) `active_duration_ms`
   - 只统计前台可见时长
4) `completion_rate`
   - 分子：本会话中，该章节已点击过的可点击词位数
   - 分母：该章节全部可点击词位数
   - 同一词位重复点击不重复计数
5) `completed_overall_rate`
   - 当前会话的客观总完成率
   - 计算方式：所有章节已点击词位总数 / 所有章节总可点击词位总数
6) `dropoff_*`
   - 表示“这次会话最后停在阅读内容哪里”
   - 只会被 `page_view / word_tap` 更新
   - 不会被反馈弹窗、更多章节弹窗等后续动作覆盖

三、Batch 1 报表示例 SQL

```sql
CREATE OR REPLACE VIEW analytics_batch1_view AS
SELECT
  anonymous_user_id AS user_id,
  session_id,
  session_started_at,
  first_word_tap_sec,
  ROUND(active_duration_ms / 1000, 1) AS duration_seconds,
  ROUND(completed_overall_rate * 100, 1) AS overall_completion_percent,
  CASE WHEN is_return_visit = 1 THEN 'Y' ELSE 'N' END AS returned
FROM analytics_sessions;
```

四、Batch 1 汇总视图（老板快速看结果）

```sql
CREATE OR REPLACE VIEW analytics_batch1_summary_view AS
SELECT
  COUNT(*) AS total_users,
  ROUND(AVG(started_user) * 100, 1) AS started_user_percent,
  ROUND(AVG(avg_time_to_first_tap_sec), 1) AS average_time_to_first_tap_sec,
  ROUND(AVG(avg_session_duration_seconds), 1) AS average_session_duration_seconds,
  ROUND(AVG(avg_completion_percent), 1) AS average_completion_percent,
  ROUND(AVG(return_user) * 100, 1) AS return_user_percent
FROM (
  SELECT
    anonymous_user_id,
    MAX(CASE WHEN first_word_tap_sec IS NOT NULL THEN 1 ELSE 0 END) AS started_user,
    AVG(CASE WHEN first_word_tap_sec IS NOT NULL THEN first_word_tap_sec END) AS avg_time_to_first_tap_sec,
    AVG(active_duration_ms / 1000) AS avg_session_duration_seconds,
    AVG(completed_overall_rate * 100) AS avg_completion_percent,
    MAX(CASE WHEN is_return_visit = 1 THEN 1 ELSE 0 END) AS return_user
  FROM analytics_sessions
  GROUP BY anonymous_user_id
) AS user_rollup;
```

字段含义：
1) `total_users`
   - 去重后的匿名用户总数
2) `started_user_percent`
   - 至少点过一次可点击单词的用户占比
3) `average_time_to_first_tap_sec`
   - 已经开始互动的用户，平均首次点击秒数
4) `average_session_duration_seconds`
   - 平均前台活跃时长（秒）
5) `average_completion_percent`
   - 平均整体完成率（百分比）
6) `return_user_percent`
   - 回访用户占比

说明：
1) 这张汇总视图按“用户”口径汇总，不按 session 口径。
2) 同一个用户有多次 session 时，会先在用户层聚合，再做整体平均。
3) 这样可以避免重度用户因为 session 太多而扭曲批次结果。
```

五、当前接入点

1) `src/App.vue`
   - 初始化全局 analytics session
2) `src/pages/ReadingPage.vue`
   - 上报 page_view / word_tap / chapter_complete
   - 注册章节总词位数
3) `src/components/ParentFeedbackModal.vue`
   - 上报 `parent_feedback`
4) `src/components/MoreChaptersModal.vue`
   - 上报 `more_chapters_response`

六、Phase 2 查询示例

1) 每个单词被点了多少次：
```sql
SELECT
  book_id,
  chapter_no,
  word,
  COUNT(*) AS tap_count
FROM analytics_events
WHERE event_type = 'word_tap'
GROUP BY book_id, chapter_no, word
ORDER BY book_id, chapter_no, tap_count DESC, word ASC;
```

2) 每次会话最后掉线到哪里：
```sql
SELECT
  anonymous_user_id,
  session_id,
  dropoff_book_id,
  dropoff_chapter_no,
  dropoff_page_no,
  dropoff_line_index,
  dropoff_position_key,
  dropoff_word,
  last_content_event_at
FROM analytics_sessions
ORDER BY session_started_at DESC;
```

3) 每章精细完成率：
```sql
SELECT
  session_id,
  anonymous_user_id,
  book_id,
  chapter_no,
  unique_tapped_positions,
  total_interactive_positions,
  completion_rate,
  is_completed
FROM analytics_session_chapters
ORDER BY updated_at DESC;
```

数据库帐号：cleverpa_tyk
数据库密码：Cv+k22A8#t3(G@&L

本地/线上数据库配置说明：
1) 线上环境直接使用 api/config.php 中的配置。
2) 本地开发时新建 api/config.local.php 覆盖部分配置（仅本地使用，别上传）。
   - 可覆盖字段：db_host、db_port
   - 示例：
     <?php
     return [
       'db_host' => 'YOUR_DB_HOST',
       'db_port' => 3306
     ];

本地开发走线上接口说明：
1) 在项目根目录创建 .env.local（仅本地使用，别上传）：
   VITE_API_BASE_URL=https://cleverpandabookclub.com
2) 前端统一请求路径：`/api/feedback.php`、`/api/tracking.php`、`/api/behavior_events.php`、`/api/analytics.php`，自动拼接 `VITE_API_BASE_URL`。
3) 本地 Vite 会直接调用线上 PHP 接口，无需本地直连数据库。
4) PHP 接口已对 http://localhost:5173 与 http://127.0.0.1:5173 放行 CORS。

数据表变更（source 标记）：
1) 新增字段：
   ALTER TABLE feedback
     ADD COLUMN source ENUM('internal','fiverr','unknown') NOT NULL DEFAULT 'unknown' AFTER session_id;
2) 前端读取 URL 参数 ?src=internal 或 ?src=fiverr，写入 localStorage 并随上报提交。

数据表变更（env 标记）：
1) 新增字段：
   ALTER TABLE feedback
     ADD COLUMN env ENUM('test','mvp','unknown') NOT NULL DEFAULT 'unknown' AFTER source;
2) 前端通过 VITE_APP_ENV 上报 env，支持 test / mvp，其它值自动变为 unknown。

数据表变更（popup_version 标记）：
1) 新增字段：
   ALTER TABLE feedback
     ADD COLUMN popup_version VARCHAR(64) NULL AFTER env;
2) 提交时写入固定值（如 v2_parent），旧数据留空视为 v1。

会话级阅读追踪（tracking）：
1) 新增表（每个 session_id 一条）：
   CREATE TABLE tracking (
     session_id VARCHAR(80) NOT NULL,
     anonymous_user_id VARCHAR(80) NOT NULL,
     total_click_count INT UNSIGNED NOT NULL DEFAULT 0,
     reached_sentence_6 TINYINT(1) NOT NULL DEFAULT 0,
     active_duration_ms BIGINT UNSIGNED NOT NULL DEFAULT 0,
     session_start_at DATETIME NOT NULL,
     last_event_at DATETIME NOT NULL,
     created_at DATETIME NOT NULL,
     updated_at DATETIME NOT NULL,
     PRIMARY KEY (session_id),
     KEY idx_tracking_anonymous_user_id (anonymous_user_id),
     KEY idx_tracking_updated_at (updated_at)
   ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
2) 新增接口：`POST /api/tracking.php`
3) 请求体（增量上报）：
   - 必填：`session_id`、`anonymous_user_id`
   - 可选：`click_count_delta`、`active_duration_delta_ms`、`reached_sentence_6`

行为追踪（return/chapter/parent_feedback）：
1) 新增表：
   CREATE TABLE behavior_events (
     id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
     event_type ENUM('return_visit','chapter_complete','parent_feedback','more_chapters_response') NOT NULL,
     session_id VARCHAR(80) NOT NULL,
     anonymous_user_id VARCHAR(80) NOT NULL,
     book_id VARCHAR(80) DEFAULT NULL,
     chapter_no INT UNSIGNED DEFAULT NULL,
     hours_since_last_visit DECIMAL(8,2) DEFAULT NULL,
     days_since_last_visit DECIMAL(8,2) DEFAULT NULL,
     liked ENUM('yes','no') DEFAULT NULL,
     comment VARCHAR(500) DEFAULT NULL,
     more_chapters_response ENUM('yes','no') DEFAULT NULL,
     more_chapters_note VARCHAR(500) DEFAULT NULL,
     event_at DATETIME NOT NULL,
     created_at DATETIME NOT NULL,
     updated_at DATETIME NOT NULL,
     PRIMARY KEY (id),
     UNIQUE KEY uniq_event_session_chapter (event_type, session_id, book_id, chapter_no),
     KEY idx_behavior_events_type_created (event_type, created_at),
     KEY idx_behavior_events_anonymous (anonymous_user_id)
   ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
2) 新增会话级汇总表（建议业务查询优先看这张，一行一个 `session_id + book_id`）：
   CREATE TABLE behavior_user_summary (
     session_id VARCHAR(80) NOT NULL,
     anonymous_user_id VARCHAR(80) NOT NULL,
     book_id VARCHAR(80) NOT NULL DEFAULT '',
     return_visit_hours DECIMAL(8,2) DEFAULT NULL,
     return_visit_days DECIMAL(8,2) DEFAULT NULL,
     completed_chapters_json LONGTEXT DEFAULT NULL,
     parent_feedback_json LONGTEXT DEFAULT NULL,
     more_chapters_json LONGTEXT DEFAULT NULL,
     created_at DATETIME NOT NULL,
     updated_at DATETIME NOT NULL,
     PRIMARY KEY (session_id, book_id),
     KEY idx_behavior_user_summary_anonymous (anonymous_user_id),
     KEY idx_behavior_user_summary_updated (updated_at)
   ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
3) 新增接口：`POST /api/behavior_events.php`
4) 事件类型：
   - `return_visit`：记录 24~72 小时回访（`hours_since_last_visit`、`days_since_last_visit`）
   - `chapter_complete`：章节完成（同会话同章节本地防重 + 数据库唯一键防重）
   - `parent_feedback`：家长反馈（`liked` + `comment`）
   - `more_chapters_response`：是否想要更多章节（`more_chapters_response` + `more_chapters_note`）
5) `api/behavior_events.php` 每次写入时会同时更新 `behavior_user_summary`：
   - 建议业务看板直接查询 `behavior_user_summary`（会话级一行）
   - `behavior_events` 保留作事件明细审计

已上线库迁移（避免字段冲突）：
1) 明细表新增字段：
   ALTER TABLE behavior_events
     ADD COLUMN more_chapters_response ENUM('yes','no') DEFAULT NULL AFTER comment,
     ADD COLUMN more_chapters_note VARCHAR(500) DEFAULT NULL AFTER more_chapters_response;
2) 更新事件枚举（如果还未包含）：
   ALTER TABLE behavior_events
     MODIFY event_type ENUM('return_visit','chapter_complete','parent_feedback','more_chapters_response') NOT NULL;
3) 将唯一键从“匿名用户维度”改成“会话维度”（若你已建过旧索引）：
   ALTER TABLE behavior_events
     DROP INDEX uniq_event_user_chapter,
     ADD UNIQUE KEY uniq_event_session_chapter (event_type, session_id, book_id, chapter_no);
4) 新增会话级汇总表（若不存在）：
   CREATE TABLE IF NOT EXISTS behavior_user_summary (
     session_id VARCHAR(80) NOT NULL,
     anonymous_user_id VARCHAR(80) NOT NULL,
     book_id VARCHAR(80) NOT NULL DEFAULT '',
     return_visit_hours DECIMAL(8,2) DEFAULT NULL,
     return_visit_days DECIMAL(8,2) DEFAULT NULL,
     completed_chapters_json LONGTEXT DEFAULT NULL,
     parent_feedback_json LONGTEXT DEFAULT NULL,
     more_chapters_json LONGTEXT DEFAULT NULL,
     created_at DATETIME NOT NULL,
     updated_at DATETIME NOT NULL,
     PRIMARY KEY (session_id, book_id),
     KEY idx_behavior_user_summary_anonymous (anonymous_user_id),
     KEY idx_behavior_user_summary_updated (updated_at)
   ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
5) 如果你已按旧结构创建过 `behavior_user_summary`（主键是 `anonymous_user_id, book_id`），请执行：
   ALTER TABLE behavior_user_summary
     ADD COLUMN session_id VARCHAR(80) NOT NULL DEFAULT '' FIRST;

   UPDATE behavior_user_summary
   SET session_id = CONCAT('legacy-', anonymous_user_id)
   WHERE session_id = '';

   ALTER TABLE behavior_user_summary
     DROP PRIMARY KEY,
     ADD PRIMARY KEY (session_id, book_id),
     ADD KEY idx_behavior_user_summary_anonymous (anonymous_user_id);
6) 如果你已不再需要旧汇总表，可删除：
   DROP TABLE IF EXISTS behavior_session_summary;

阅读内容 JSON（兼容新旧）：
1) 旧格式继续支持：
   - `items[].zhLines` + `items[].enLines` + `interactiveWords[]`
2) 新格式（推荐，schemaVersion=`cp-book-v1`）：
   - `chapter.pages[].zh`（字符串或数组）
   - `chapter.pages[].enRich`（字符串或数组）
   - `enRich` 标注语法：
     - `{g|word}` 绿色可点击（主语/主要动词）
     - `{b|word}` 蓝色可点击（限定词/名词/专有名词/代词）
3) 音频目录保持全局：
   - `audio.baseUrl` 默认 `/audio/`
   - 不按图书/章节分目录，文件通过 `audio.manifest` 映射。
